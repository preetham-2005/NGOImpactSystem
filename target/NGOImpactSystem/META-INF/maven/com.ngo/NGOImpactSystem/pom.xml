<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <groupId>com.ngo</groupId>
  <artifactId>NGOImpactSystem</artifactId>
  <version>1.0</version>
  <packaging>war</packaging>

  <!-- ✅ Dependencies -->
  <dependencies>
    <!-- MySQL Connector -->
    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
      <version>8.0.33</version>
    </dependency>
  </dependencies>

  <!-- ✅ Build configuration (IMPORTANT) -->
  <build>
    <finalName>NGOImpactSystem</finalName>

    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-war-plugin</artifactId>
        <version>3.4.0</version>
        <configuration>
          <!-- ✅ This tells Maven your web pages are in WebContent folder -->
          <warSourceDirectory>WebContent</warSourceDirectory>

          <!-- ✅ Do not fail if web.xml is not found in Maven path -->
          <failOnMissingWebXml>false</failOnMissingWebXml>
        </configuration>
      </plugin>
    </plugins>

  </build>

</project>
